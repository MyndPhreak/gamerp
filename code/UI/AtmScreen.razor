@using Sandbox;
@using Sandbox.UI;
@using GameRP.Economy;
@using GameRP.Systems;
@using System;
@using System.Threading.Tasks;

@namespace GameRP.UI
@inherits PanelComponent

<root style="@RootStyle">
    <div style="position: absolute; left: 0; top: 0; width: 100%; height: 100%; background-color: rgba(0,0,0,0.8);" @onclick=@Close></div>

    <div style="position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%); width: 800px; background-color: rgba(30,30,40,0.95); border-radius: 16px; padding: 48px; display: flex; flex-direction: column; gap: 24px;">
        @if (IsLoading)
        {
            <div style="display: flex; flex-direction: column; align-items: center; gap: 16px; padding: 48px;">
                <div style="width: 48px; height: 48px; border: 4px solid rgba(255,255,255,0.2); border-top-color: #667eea; border-radius: 50%;"></div>
                <div style="font-size: 16px; font-weight: 600; color: white;">Loading...</div>
            </div>
        }
        else if (WalletData != null)
        {
            <div style="display: flex; justify-content: space-between; align-items: center;">
                <div style="font-size: 36px; font-weight: 700; color: white;">ATM</div>
                <div style="width: 48px; height: 48px; background-color: rgba(255,255,255,0.1); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 32px; color: white; cursor: pointer;" @onclick=@Close>×</div>
            </div>

            <div style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 24px; border-radius: 8px; text-align: center; display: flex; flex-direction: column; gap: 12px;">
                <div style="font-size: 18px; color: rgba(255,255,255,0.8); text-transform: uppercase; letter-spacing: 2px;">Balance</div>
                <div style="font-size: 56px; font-weight: 800; color: white;">$@WalletData.Balance.ToString("N2")</div>
            </div>

            @if (!ShowAmountInput)
            {
                <div style="display: flex; gap: 16px;">
                    <div style="flex: 1; padding: 28px; border-radius: 12px; cursor: pointer; font-size: 20px; font-weight: 700; color: white; text-transform: uppercase; letter-spacing: 2px; background: linear-gradient(135deg, #4CAF50 0%, #45a049 100%); text-align: center;" @onclick=@(() => ShowInput(true))>DEPOSIT</div>
                    <div style="flex: 1; padding: 28px; border-radius: 12px; cursor: pointer; font-size: 20px; font-weight: 700; color: white; text-transform: uppercase; letter-spacing: 2px; background: linear-gradient(135deg, #f44336 0%, #d32f2f 100%); text-align: center;" @onclick=@(() => ShowInput(false))>WITHDRAW</div>
                </div>
            }
            else
            {
                <div style="display: flex; flex-direction: column; gap: 16px;">
                    <div style="color: white; font-size: 14px; font-weight: 600; text-transform: uppercase; letter-spacing: 1px;">Amount</div>
                    <input type="number" @bind="Amount" placeholder="0.00" step="0.01" style="background-color: rgba(255,255,255,0.1); border: 3px solid rgba(255,255,255,0.2); border-radius: 12px; padding: 24px; font-size: 36px; font-weight: 700; color: white; text-align: center;" />

                    <div style="display: flex; gap: 12px;">
                        <div style="flex: 1; background-color: rgba(255,255,255,0.1); border: 2px solid rgba(255,255,255,0.2); border-radius: 8px; padding: 16px; cursor: pointer; font-size: 18px; font-weight: 600; color: white; text-align: center;" @onclick=@(() => Amount = 100)>$100</div>
                        <div style="flex: 1; background-color: rgba(255,255,255,0.1); border: 2px solid rgba(255,255,255,0.2); border-radius: 8px; padding: 16px; cursor: pointer; font-size: 18px; font-weight: 600; color: white; text-align: center;" @onclick=@(() => Amount = 500)>$500</div>
                        <div style="flex: 1; background-color: rgba(255,255,255,0.1); border: 2px solid rgba(255,255,255,0.2); border-radius: 8px; padding: 16px; cursor: pointer; font-size: 18px; font-weight: 600; color: white; text-align: center;" @onclick=@(() => Amount = 1000)>$1,000</div>
                    </div>

                    <div style="display: flex; gap: 16px; margin-top: 8px;">
                        <div style="flex: 1; padding: 20px; border-radius: 12px; cursor: pointer; font-size: 18px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; color: white; background-color: rgba(255,255,255,0.1); border: 2px solid rgba(255,255,255,0.3); text-align: center;" @onclick=@CancelInput>Cancel</div>
                        <div style="@ConfirmButtonStyle" @onclick=@ProcessTransaction>@(IsDeposit ? "Deposit" : "Withdraw")</div>
                    </div>
                </div>
            }

            @if (!string.IsNullOrEmpty(Message))
            {
                <div style="@MessageStyle">@Message</div>
            }
        }
    </div>
</root>

@code
{
    private bool IsOpen { get; set; } = false;
    private long SteamId { get; set; }
    private WalletData WalletData { get; set; }
    private bool IsLoading { get; set; } = true;
    private bool ShowAmountInput { get; set; } = false;
    private bool IsDeposit { get; set; } = false;
    private decimal Amount { get; set; } = 0;
    private string Message { get; set; } = "";
    private string MessageClass { get; set; } = "";
    private bool _isProcessing = false;

    private string RootStyle => IsOpen
        ? "position: absolute; left: 0; top: 0; width: 100%; height: 100%; z-index: 100; opacity: 1; pointer-events: all;"
        : "position: absolute; left: 0; top: 0; width: 100%; height: 100%; z-index: 100; opacity: 0; pointer-events: none;";

    private string ConfirmButtonStyle => IsDeposit
        ? "flex: 1; padding: 20px; border-radius: 12px; cursor: pointer; font-size: 18px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; color: white; background: linear-gradient(135deg, #4CAF50 0%, #45a049 100%); text-align: center;"
        : "flex: 1; padding: 20px; border-radius: 12px; cursor: pointer; font-size: 18px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; color: white; background: linear-gradient(135deg, #f44336 0%, #d32f2f 100%); text-align: center;";

    private string MessageStyle => MessageClass == "success"
        ? "margin-top: 16px; padding: 16px; border-radius: 8px; font-size: 14px; font-weight: 600; background-color: rgba(76,175,80,0.2); border: 2px solid #4CAF50; color: #4CAF50; text-align: center;"
        : "margin-top: 16px; padding: 16px; border-radius: 8px; font-size: 14px; font-weight: 600; background-color: rgba(244,67,54,0.2); border: 2px solid #f44336; color: #f44336; text-align: center;";

    public async void Open(long steamId)
    {
        if (IsOpen)
        {
            Log.Info("[AtmScreen] Already open, ignoring");
            return;
        }

        Log.Info($"[AtmScreen] Opening for Steam ID: {steamId}");
        SteamId = steamId;
        IsOpen = true;
        ShowAmountInput = false;
        Amount = 0;
        Message = "";
        Mouse.Visibility = MouseVisibility.Visible;
        await LoadWallet();
    }

    private async Task LoadWallet()
    {
        IsLoading = true;
        StateHasChanged();

        try
        {
            WalletData = await EconomySystem.Api.GetWalletAsync(SteamId);
            Log.Info($"[AtmScreen] Wallet loaded: ${WalletData?.Balance}");
        }
        catch (Exception ex)
        {
            Log.Error($"[AtmScreen] Error: {ex.Message}");
            Message = "Failed to load wallet";
            MessageClass = "error";
        }
        finally
        {
            IsLoading = false;
            StateHasChanged();
        }
    }

    private void ShowInput(bool isDeposit)
    {
        IsDeposit = isDeposit;
        ShowAmountInput = true;
        Amount = 0;
        Message = "";
    }

    private void CancelInput()
    {
        ShowAmountInput = false;
        Amount = 0;
        Message = "";
    }

    private async void ProcessTransaction()
    {
        if (Amount <= 0)
        {
            Message = "Amount must be greater than 0";
            MessageClass = "error";
            StateHasChanged();
            return;
        }

        if (_isProcessing)
        {
            Log.Warning("[AtmScreen] Transaction already in progress, ignoring duplicate");
            return;
        }

        _isProcessing = true;
        IsLoading = true;
        Message = "";
        StateHasChanged();

        try
        {
            Log.Info($"[AtmScreen] Processing {(IsDeposit ? "deposit" : "withdrawal")} of ${Amount}");
            WalletData result = IsDeposit
                ? await EconomySystem.Deposit(SteamId, Amount, "ATM Deposit")
                : await EconomySystem.Withdraw(SteamId, Amount, "ATM Withdrawal");

            if (result != null)
            {
                WalletData = result;
                Message = $"Success! New balance: ${result.Balance:N2}";
                MessageClass = "success";
                ShowAmountInput = false;
            }
            else
            {
                Message = "Transaction failed";
                MessageClass = "error";
            }
        }
        catch (Exception ex)
        {
            Log.Error($"[AtmScreen] Transaction error: {ex.Message}");
            Message = $"Error: {ex.Message}";
            MessageClass = "error";
        }
        finally
        {
            IsLoading = false;
            _isProcessing = false;
            StateHasChanged();
        }
    }

    private void Close()
    {
        Log.Info("[AtmScreen] Closing");
        IsOpen = false;
        ShowAmountInput = false;
        Amount = 0;
        Message = "";
        Mouse.Visibility = MouseVisibility.Auto;
    }

    protected override int BuildHash() => System.HashCode.Combine(IsOpen, WalletData?.Balance, IsLoading, ShowAmountInput, Message, _isProcessing);
}
